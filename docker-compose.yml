services:
  dodgerblue:
    build: .
    command: gunicorn dodgerblue.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - postgres
  
  postgres:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - .env.db
    command: postgres -c listen_addresses=*
    ports:
      - "5432:5432"

  test:
    build: .
    command: pytest --disable-warnings
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - postgres
  
volumes:
  postgres_data: