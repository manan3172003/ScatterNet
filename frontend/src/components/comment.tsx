import {Comment} from "@/types/ModelTypes.tsx";

interface CommentProps {
    comment: Comment;
    className: string;
}

const Comment: React.FC<CommentProps> = ({
    comment,
    className
}) => {


}